cmake_minimum_required(VERSION 3.5)
project(amr)

# Find ament_cmake, which is required for all ROS 2 packages
find_package(ament_cmake REQUIRED)

# Mark the package as an ament package
ament_package()
